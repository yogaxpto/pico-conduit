[package]
name    = "pico-conduit-macros"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Proc-macro helpers for pico-conduit tests"

[lib]
proc-macro = true

[dependencies]
quote = "1"
syn   = { version = "3", features = ["full"] }
